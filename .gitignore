# Backend
backend/node_modules/
backend/dist/
backend/.env
backend/.npm-cache/
backend/prisma/.env

# OS
.DS_Store
Thumbs.db

# Ferramenta local só (admin secret); não versionar — deploy excluído no .vercelignore
convites.html
